"use client";

export default function LoginPage() {
  return (
    <div
      className={`flex flex-col items-center justify-center h-screen transition-colors duration-300 bg-light_background text-black dark:bg-dark_background dark:text-white`}
    >
      <h1>Login</h1>
    </div>
  );
}
